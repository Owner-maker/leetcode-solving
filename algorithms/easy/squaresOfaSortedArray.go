package main

import "fmt"

func sortedSquares(nums []int) []int {
	res := make([]int, len(nums), len(nums))

	left := 0
	right :=
}

func main() {
	f := sortedSquares([]int{-4, -1, 0, 3, 10})
	fmt.Print(f)
}
